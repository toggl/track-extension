os: linux
dist: xenial
language: node_js
node_js:
  - 10.18.0

jobs:
  include:
    - stage: build
      script:
        - npm run lint:ci
        - npm run build
    - stage: release
      deploy:
        provider: script
        script:
          - npx semantic-release
